<div class="profile-banner">
<div class="row">
  <div class="col s6 m6">
    <img src="http://cps-static.rovicorp.com/3/JPG_500/MI0000/049/MI0000049473.jpg?partner=allrovi.com" class="avatar-profile">
  </div>
  <div class="col s6 m6" align="right">
    <strong>
      <h5><%= current_user.user_name %></h5>
      <%= link_to "Orders", orders_path, method: :get %>
      <h6>
        <span class='user-like-count'>
          <%= current_user.liked_items.count %>
        </span>
        <br/>likes
      </h6>
    </strong>
  </div>
  </div>
</div>

<div class="row">
 <% current_user.liked_items.each do |c| %>
    <div class="col s6 m6 l3">
      <div class="card" data-item="<%= c.id %>">
        <a class="modal-trigger  waves-light " href="#modal<%= c.id %>">
          <div class="card-image">
            <%= image_tag c.tumbnail_img %>
          </div>
        </a>

        <div class="card-content">
          <div class="card-title"><%= c.name %></div>
          <% item_like = current_user.item_likes.find{ |item_like| item_like if item_like.item_id == c.id } %>
          <p>Price: $<%= c.price %></p>
          <span class="like-count"><%= c.item_likes.count %></span> likes
        </div>

        <div class="card-action">
          <div class="like-btn">
            <% if item_like.nil? %>
              <%= link_to 'like', item_item_likes_path(c.id, item_like), method: :post, remote: true %>
            <% end %>
          </div>
          <div class="unlike-btn">
            <% unless item_like.nil? %>
              <%= link_to 'unlike', item_item_like_path(c.id, item_like), method: :delete, remote: true %><br/>
              <% end %>
          </div>
        </div>
      </div>
    </div>
  <% end %>
</div>


<!-- Modal Structure -->
<% current_user.liked_items.each do |c| %>
  <div id="modal<%= c.id %>" class="modal profil item" data-item="<%= c.id %>">
    <% item_like = current_user.item_likes.find{ |item_like| item_like if item_like.item_id == c.id } %>
    <div class="modal-content">
      <div class="center">
        <h4><%= c.name %></h4>
          <%= image_tag c.tumbnail_img, class: "modal-image" %><br/>
          <br/>
          Price: $<%= c.price %> <br/>
          <span class="like-count"><%= c.item_likes.count %></span> likes
          <br/>
          <br/>
      </div>
      <div class= "row">
        <div class= "col s6 m6">
          Description: <%= c.description %><br/>
        </div>
        <div class= "col s6 m6">
          Gender: <%= c.gender %><br/>
          Brand: <%= c.brand.name %><br/>
          Colors: <%= c.colors.map(&:name).join(', ') %><br/>
        </div>
      </div>
    </div>

    <div class="modal-footer ">
      <div class="like-btn modal-close btn-flat">
        <% if item_like.nil? %>
          <%= link_to 'like', item_item_likes_path(c.id, item_like), method: :post, remote: true %>
        <% end %>
      </div>
      <div class="unlike-btn modal-close btn-flat">
        <% unless item_like.nil? %>
          <%= link_to 'unlike', item_item_like_path(c.id, item_like), method: :delete, remote: true %><br/>
        <% end %>
      </div>
      <a href="#!" class=" modal-action modal-close waves-effect waves-red btn-flat">Close</a>
    </div>
  </div>
<% end %>
</div>


<script>
    $(document).ready(function(){
    // the "href" attribute of .modal-trigger must specify the modal ID that wants to be triggered
    $('.modal').modal();
  });
</script>





























<!--


<% current_user.liked_items.each do |item| %>
<div class='profil item' data-item='<%= item.id %>'>
  <%= item.tumbnail_img %><br/>
  <%= item.name %><br/>
  <%= item.price %> $<br/>
  <%= item.description %><br/>
  <%= item.gender %><br/>
  <%= item.brand.name %><br/>
  <%= item.colors.map(&:name).join(', ') %><br/>
  <% item_like = current_user.item_likes.find{ |item_like| item_like if item_like.item_id == item.id} %>
  <%= item.item_likes.count %> likes<br/>
  <%= link_to 'unlike', item_item_like_path(item.id, item_like), remote: true, method: :delete %><br/>
  <%= link_to new_order_path(item_id: item.id), method: :get do %>
  <div class="btn btn-primary">Order</div>
  <br/>
  <% end %>
</div>
<% end %> -->

